# 서버 포트 설정
PORT=3000

# MySQL 데이터베이스 설정
DB_HOST=127.0.0.1      # 로컬에서 실행 중인 MySQL 서버 (localhost와 동일)
DB_USER=root           # MySQL 기본 사용자 이름
DB_PASSWORD=djaqndl5795
DB_NAME=job_portal     # 데이터베이스 이름 (예: job_portal)

# JWT (JSON Web Token) 설정
JWT_SECRET=supersecretkey123   # JWT 토큰 생성에 사용할 비밀 키
REFRESH_SECRET=refreshkey456   # Refresh 토큰 생성에 사용할 비밀 키
TOKEN_EXPIRES_IN=7d            # Access 토큰 만료 시간 (1시간)

# 참고: 비밀번호는 보안상 공개하지 않도록 주의!
